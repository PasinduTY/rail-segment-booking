# Copy this file to .env and set real values before running `docker compose up`.
# .env is git-ignored - never commit real credentials.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=railbooking
